{
  "name": "cms-miniapp-wp",
  "version": "1.0.0",
  "scripts": {
    "build": "cd server && npm install && npm run build",
    "start": "node dist/app.js",
    "db:migrate": "node dist/db/migrate.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
